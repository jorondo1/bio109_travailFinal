Auteurs: Jonathan Rondeau-Leclaire, Chloé Girard

PROGRAMME chargement_données
LIRE quadrats.txt

ECRIRE etats

ECRIRE etat_0()
SI (abba > 0 | piru > 0) & acsa==0 & beal==0 & acpe==0 & fagr==0
	ALORS etat = "B"
	SINON SI abba==0 & piru==0 & (acsa>0 | beal>0 | acpe>0 | fagr>0)
		ALORS etat = "T"
		SINON SI (abba > 0 | piru > 0) & (acsa>0 | beal>0 | acpe>0 | fagr>0)
			ALORS etat = "M"
			SINON etat = "R"
FIN

LIRE matrice_transitions.txt
ECRIRE transmx



PROGRAMME fonctionEtat	   # Fonction pour tirer au hasard l'état t+1
ECRIRE etat.fun()
	DEFINIR p
	DEFINIR res
RETOURNER etat
		
*****
DRAW etatT1 ou COMPUTE N_t1 # dans les notes de cours c'est écrit d'utiliser DRAW, mais dans la page de dépôt c'est écrit COMPUTE N_t1
*****


PROGRAMME simul.fun2
ECRIRE simul.fun2()	   # Fonction pour simuler N pas de temps
	SEED (123)
		DEFINIR composition
		DEFINIR pas_t
		DEFINIR liste_pas
		POUR i in 1:(N) {
			POUR j in 1:longueur(pas_t)
			}
			DEFINIR t+1
		DEFINIR pas_t comme t+1
	RETOURNER
	
		

*****	
Entier Matrice (N, mX)
POUR compteur <- 0, N, PAS 1
etatTMatrice <- etat.fun(etatT)
???????????
FIN
*****
